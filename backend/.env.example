# LLM Configuration
ANTHROPIC_BASE_URL=
ANTHROPIC_AUTH_TOKEN=
ANTHROPIC_MODEL=

# Report settings (optional, defaults are fine)
# REPORT_MONTHS=6
# QUERY_COMPARE_DAYS=28

# Google API Credentials (required for PageSpeed Insights API, which requires an API key, not OAuth)
GOOGLE_API_KEY = 
